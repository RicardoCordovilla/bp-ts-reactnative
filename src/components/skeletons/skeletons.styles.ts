import { StyleSheet } from "react-native";

const styles = StyleSheet.create({  
    skeletonRegister_container: {
        backgroundColor: '#bbb',
        padding: 12,
        flexDirection: 'row',
    }
});